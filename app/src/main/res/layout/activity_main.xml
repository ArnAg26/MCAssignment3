<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:orientation="vertical">

    <!-- Location Selection -->
    <TextView
        android:id="@+id/tvCurrentLocation"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Select Current Location"
        android:textSize="18sp"
        android:paddingBottom="8dp" />

    <RadioGroup
        android:id="@+id/radioGroupLocation"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <RadioButton
            android:id="@+id/radioLocationA"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Next to Router" />

        <RadioButton
            android:id="@+id/radioLocationB"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Center of Room" />

        <RadioButton
            android:id="@+id/radioLocationC"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Next Room" />
    </RadioGroup>

    <!-- Buttons for Logging -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="16dp">

        <Button
            android:id="@+id/btnStartLogging"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="Start Logging" />

        <Button
            android:id="@+id/btnStopLogging"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="wrap_content"
            android:text="Stop Logging"
            android:layout_marginStart="8dp" />
    </LinearLayout>

    <!-- Save to CSV -->
    <Button
        android:id="@+id/btnSaveToCSV"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Save Data to CSV"
        android:layout_marginTop="12dp"/>

    <!-- Show Summary -->
    <Button
        android:id="@+id/btnShowSummary"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Show Summary"
        android:layout_marginTop="12dp" />

    <!-- Summary Display -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="16dp">

        <TextView
            android:id="@+id/tvSummary"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Summary will appear here..."
            android:textSize="16sp" />
    </ScrollView>
</LinearLayout>
